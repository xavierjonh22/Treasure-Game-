# Treasure-Game-
A board game calling a user to find random treasure on a grid that the user creates the dimensions for. 

Features: 
- Generates Treasure in random spots when the user makes the grid size
- Checks for Treasure on called on parts of the grid
- Displays Grid once all treasure is found
